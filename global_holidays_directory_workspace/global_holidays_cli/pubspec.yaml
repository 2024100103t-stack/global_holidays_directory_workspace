name: global_holidays_cli

description: Command-line interface for Global Holidays Directory.

version: 1.0.0

publish_to: none

resolution: workspace

environment:
  sdk: '^3.8.1'

dependencies:
  http: ^1.2.0
  logging: ^1.2.0
  terminal_colors:
    path: ../terminal_colors
  global_holidays_api:
    path: ../global_holidays_api

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0